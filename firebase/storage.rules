rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // 開発段階では全てのアクセスを拒否（セキュリティ最優先）
    // Phase 2で必要に応じてルールを実装
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}